yol ayrımı olduğu zamanlarda if veya else kullanılır.

if(koşul) {
    //kodlar
}

else{
    //kodlar
}

consolda eğer true çıktısı alınıyorsa if deki kodlar çalışır else çalışmaz false çıktısı alınırsa else deki kodlar çalışır if deki kodlar çalışmaz.